# ER図

## 改訂履歴

| Version | Date       | Author | Changes |
|---------|------------|--------|---------|
| 0.2.1   | 2026-03-23 | OpenAI | ユーザー作成型サービスを見据え、`site` の所有者・公開状態を反映 |
| 0.2.0   | 2026-03-23 | OpenAI | 拡張性重視で ER 図を更新。作品中間テーブル、所属履歴、別名、コール種別マスタを追加 |
| 0.1.0   | 2026-03-22 | OpenAI | 初版作成 |

---

## 1. ER図（Mermaid）

```mermaid
erDiagram
    site ||--|| site_theme_setting : has
    site ||--o{ idol_group : has
    site ||--o{ member : has
    site ||--o{ member_group_history : has
    site ||--o{ single_release : has
    site ||--o{ song : has
    site ||--o{ release_song : has
    site ||--o{ song_alias : has
    site ||--o{ member_alias : has
    site ||--o{ call_type_master : has

    idol_group ||--o{ member_group_history : includes

    member ||--o{ member_group_history : belongs
    member ||--o{ member_alias : has
    member ||--o{ song_member : assigned_to

    single_release ||--o{ release_song : contains

    song ||--o{ release_song : linked_to
    song ||--o{ song_member : has
    song ||--o{ song_alias : has
    song ||--o{ song_call_block : has

    song_call_block ||--o{ song_call_line : has
    song_call_line ||--o{ song_call_item : has

    call_type_master ||--o{ song_call_item : classifies

    site {
        bigint id PK
        varchar site_key UK
        varchar site_name
        varchar idol_name
        bigint owner_user_id
        varchar visibility
        varchar status
        boolean is_seeded_site
        timestamp created_at
        timestamp updated_at
    }

    site_theme_setting {
        bigint id PK
        bigint site_id FK
        varchar primary_color_hex
        varchar secondary_color_hex
        varchar accent_color_hex
        timestamp created_at
        timestamp updated_at
    }

    idol_group {
        bigint id PK
        bigint site_id FK
        varchar group_name
        varchar group_kana
        varchar official_name
        int display_order
        timestamp created_at
        timestamp updated_at
    }

    member {
        bigint id PK
        bigint site_id FK
        varchar member_name
        varchar member_name_kana
        date birthday
        varchar member_color
        varchar member_color_hex
        varchar birthplace
        varchar short_bio
        varchar profile_image_url
        int display_order
        varchar status
        timestamp created_at
        timestamp updated_at
    }

    member_group_history {
        bigint id PK
        bigint site_id FK
        bigint member_id FK
        bigint group_id FK
        varchar role_type
        varchar generation_label
        date joined_on
        date left_on
        boolean is_primary
        int display_order
        timestamp created_at
        timestamp updated_at
    }

    single_release {
        bigint id PK
        bigint site_id FK
        varchar release_type
        varchar title
        varchar title_kana
        date release_date
        varchar catalog_number
        varchar jacket_image_url
        int display_order
        timestamp created_at
        timestamp updated_at
    }

    song {
        bigint id PK
        bigint site_id FK
        bigint group_id FK
        varchar title
        varchar title_kana
        varchar title_roman
        varchar description
        boolean has_call_data
        int display_order
        timestamp created_at
        timestamp updated_at
    }

    release_song {
        bigint id PK
        bigint site_id FK
        bigint release_id FK
        bigint song_id FK
        int track_number
        int disc_number
        boolean is_primary
        boolean is_title_track
        int display_order
        timestamp created_at
        timestamp updated_at
    }

    song_member {
        bigint id PK
        bigint song_id FK
        bigint member_id FK
        varchar role_type
        int display_order
        timestamp created_at
        timestamp updated_at
    }

    song_alias {
        bigint id PK
        bigint site_id FK
        bigint song_id FK
        varchar alias_type
        varchar alias_text
        varchar normalized_text
        int display_order
        timestamp created_at
        timestamp updated_at
    }

    member_alias {
        bigint id PK
        bigint site_id FK
        bigint member_id FK
        varchar alias_type
        varchar alias_text
        varchar normalized_text
        int display_order
        timestamp created_at
        timestamp updated_at
    }

    call_type_master {
        bigint id PK
        bigint site_id FK
        varchar call_type_code
        varchar call_type_label
        varchar color_hex
        varchar icon_key
        varchar description
        int display_order
        boolean is_active
        timestamp created_at
        timestamp updated_at
    }

    song_call_block {
        bigint id PK
        bigint song_id FK
        varchar block_type
        varchar block_label
        int order_no
        timestamp created_at
        timestamp updated_at
    }

    song_call_line {
        bigint id PK
        bigint block_id FK
        int line_no
        text lyrics
        varchar note
        timestamp created_at
        timestamp updated_at
    }

    song_call_item {
        bigint id PK
        bigint call_line_id FK
        varchar call_type_code FK
        varchar call_text
        varchar style_color_hex
        int order_no
        timestamp created_at
        timestamp updated_at
    }
```

---

## 2. ER図の補足

### 2.1 サイト起点
`site` を基点に設計することで、今後 =LOVE 以外のアイドルサイトを同じ基盤で増やせる。  
また、`owner_user_id`, `visibility`, `status`, `is_seeded_site` を持つことで、後日のユーザー作成サイト、下書きサイト、運営初期サイトの区別にも対応しやすい。

### 2.2 楽曲と作品
`song` と `single_release` は直接結ばず、`release_song` を介して関連付ける。  
これにより、同一楽曲の複数作品収録や代表作品管理に対応できる。

### 2.3 メンバー所属
メンバーの所属は `member_group_history` を正とする。  
これにより、兼任、卒業、再加入、期変更に対応できる。

### 2.4 検索別名
別名は `song_alias`, `member_alias` へ分離し、本体テーブルの列増加を防ぐ。

### 2.5 コール表現
コールは単純な文字列 1 本で持たず、以下の階層で表現する。

- `song`
- `song_call_block`
- `song_call_line`
- `song_call_item`

さらに、`call_type_master` を参照することで、色・ラベル・アイコンをサイトごとに拡張できる。
